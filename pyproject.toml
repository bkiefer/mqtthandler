[project]
name = "mqtthandler"
version = "0.1.4"
description = "A convenience wrapper arount paho MQTT and a MQTT message recorder"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "paho-mqtt>=2.1.0",
    "pyyaml>=6.0",
]
